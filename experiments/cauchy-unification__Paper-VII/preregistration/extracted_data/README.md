# Extracted Data Notes

This directory now contains the numeric datasets used in the first locked 12-domain extension dry run, which was executed locally on 17 March 2026 before any OSF timestamp.

Rules:

- Do not describe the existing `extension_data.json` file as a preregistered extraction.
- Keep the current dry-run data separate from any future OSF-timestamped extension packet.
- Maintain a separate execution log for any future prospective extraction and fitting runs.
