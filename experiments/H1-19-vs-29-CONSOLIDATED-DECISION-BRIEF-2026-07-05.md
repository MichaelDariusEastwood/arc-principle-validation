# The 19-vs-29 convergence count — ONE decision, five surfaces (consolidated decision-support brief)

**Lane:** T2-research · 2026-07-05 · DECISION-SUPPORT ONLY. The count decision is T4/operator-owned (LAW 4 — T4 owns the convergence register). This brief consolidates the inputs so ONE decision propagates everywhere in a single pass, rather than five lanes chasing five flags. Recommendation included; the call is not mine.

## The single question
Is the canonical convergence count **19** (post-dedup structural convergences) — and does it survive the H1 independence stress-test, or does it move (e.g. rows 13/14 double-count + 2 predictions)?

## Why this is ONE decision, not five (everything it governs)
| Surface | Current state | What the decision resolves |
|---|---|---|
| **1. Canonical register** (`research-canonical-facts.json`) | `headline_count_verified: 19` | Confirms or revises the single source of truth |
| **2. credential.html** (public-facing) — F15 | Contradicts ITSELF: "19 convergences" ×2 AND "THE 29 CONVERGENCES" / "29 institutions" ×4 (lines 44, 155, 157, 199) | The public credibility doc gets reconciled to the agreed number |
| **3. truth-gate count-drift** (10 violations) | 10 `count-drift` hits across internal docs | These clear once the agreed count propagates |
| **4. merge guard** (`restore-29` branch) | Held OUT of origin/main ancestry; main stays 19 | Confirms whether restore-29 stays excluded (if 19) or is reconsidered |
| **5. H1 independence stress-test** | Open: do rows 13/14 double-count? do the 2 predictions count as convergences? | The analytical basis for whether 19 is final |

## The evidence (LAW 1 + LAW 2)
- **For 19:** the canonical register verdict is 19 structural convergences after dedup. Consistent with F1 ("31→≈24" pre-dedup shrinkage), F11 (count-variants), and HRIH (states 19). The 29 is the **pre-dedup** higher count.
- **The 29's content:** credential.html lists 29 "institutions" (AI labs, quantum, physics, semiconductor, VC, math logic, export law, Catholic Church, UN…). If 19 is correct, ~10 of these are dedup'd (same structural convergence counted via multiple institutions) — the LIST needs mapping to the 19, not just a number swap.
- **Honest uncertainty:** H1 has NOT been independently adjudicated. If the independence test found rows 13/14 are genuinely distinct (not double-counts), 19 could rise; if the 2 predictions don't qualify as realised convergences, it could fall. The number is defensible at 19 but not yet stress-test-final.

## Recommendation (deferred to T4/operator)
1. **Adjudicate H1 first** (independence of rows 13/14 + prediction-status) → lock the ONE number.
2. **Propagate in a single pass**: register → credential.html (retitle §4, fix the institution list to map to the locked count, fix "PNAS Nexus validates / 29 institutions" row) → clear the 10 count-drift gate hits → confirm merge-guard posture (restore-29 stays excluded if the locked number is 19).
3. Do NOT hard-code any surface to a number before H1 is locked — that just moves the contradiction around.

## Route
→ T4/operator (owns the count + H1 + register). Consolidates F1, F8, F11, F15, H1, count-drift, and the merge guard into one decision. When decided, T2 can supply the credential institution-list→convergence mapping on request (doc-hygiene support, not register authoring).
