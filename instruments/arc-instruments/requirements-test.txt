# Observed test baseline (6 September 2026): the suite passes 515 tests with none failing under these
# versions on Python 3.9.6, run as `python3 -m pytest tests/ -q` from `instruments/arc-instruments`.
# Pins name what was actually run, never what is newest: a pin that has not been run is a claim.
numpy==2.0.2
scipy==1.13.1
pytest==8.4.2
